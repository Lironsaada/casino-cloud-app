apiVersion: v1
kind: Service
metadata:
  name: {{ include "casino-app.fullname" . }}
  labels:
    {{- include "casino-app.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: {{ .Values.service.targetPort }}
      protocol: TCP
      name: http
  selector:
    {{- include "casino-app.selectorLabels" . | nindent 4 }}